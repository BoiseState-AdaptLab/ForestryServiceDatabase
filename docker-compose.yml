version: '3.3'
services:
  web:
    restart: always
    build: ./web-app
    ports:
        - "3000:3000"
  postgres:
    build: ./db
    ports:
      - "35432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: ForestServiceDB

# Names our volume
volumes:
  my-db: